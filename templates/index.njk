<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Nos produits</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <div class="max-w-4xl mx-auto py-10">
    <h2 class="text-3xl font-bold mb-8 text-center text-gray-700">Offres d'emploi</h2>
    <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
      {% for job in jobs %}
        <li class="bg-white rounded-lg shadow p-6 flex flex-col">
          <div class="flex justify-between items-start mb-4">
            <h3 class="text-xl font-semibold text-gray-800">{{ job.title }}</h3>
            <span class="bg-blue-100 text-blue-800 text-sm font-semibold px-2.5 py-0.5 rounded">{{ job.location }}</span>
          </div>
          <div class="mb-4">
            <p class="text-gray-600">{{ job.company }}</p>
            <span class="text-sm text-gray-500">{{ job.type }}</span>
          </div>
          <p class="text-gray-700 mb-4">{{ job.description|truncate(100) }}</p>
          <div class="flex justify-between items-center">
            <span class="text-sm text-gray-500">{{ job.date }}</span>
            <a href="{{ job.url }}" class="bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded">Postuler</a>
          </div>
        </li>
      {% endfor %}
    </ul>
  </div>
</body>
</html>